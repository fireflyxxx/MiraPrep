spring:
  config:
    activate:
      on-profile: prod

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS}
  internal-token: ${AI_INTERNAL_TOKEN}
  auth:
    verification:
      mail-mode: ${MAIL_MODE:smtp}
      mail-from: ${MAIL_FROM}
