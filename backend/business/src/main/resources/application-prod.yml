spring:
  config:
    activate:
      on-profile: prod

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS}
  internal-token: ${AI_INTERNAL_TOKEN}
  share:
    base-url: ${SHARE_BASE_URL}
  auth:
    verification:
      mail-mode: ${MAIL_MODE:smtp}
      mail-from: ${MAIL_FROM}
